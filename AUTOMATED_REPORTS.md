# 每月自動分析與 Excel 資料包

首頁維持「完整月報」；「趨勢重點」最上方提供該月報告，可展開或開啟獨立頁面。
顯示、報告、匯出、GitHub 手動輸入皆用西元 `YYYY/MM`。原始 MOPS 歷史檔名及爬蟲儲存索引仍使用民國識別碼以保持相容，不重寫原始公告。

## 自動發布

`Generate Monthly Analysis` 在金控／市場更新流程完成後執行，也接受資料或產生器變更、每月 8–20 日的備援排程及手動執行。它直接提交 `docs/reports/`，沒有 PR、人工審查或核准關卡。

- 沿用既有 GitHub Secret `ANTHROPIC_API_KEY`，不將金鑰送到前端。
- 最新月呼叫 Claude 產生四段新聞與市場解讀；歷史月自動建立數據分析＋既有新聞版。手動指定歷史月份也可呼叫 AI。
- 金額、表格、變化與排名由 `docs/analysis-pack.js` 統一計算，AI 不計算財務數字。模型只能輸出定性文字並引用有效證據識別碼，數字另由表格呈現。
- AI 無金鑰、失敗或格式驗證未過時，仍發布數據分析＋新聞摘要版，保留原因類型，不公開例外內容或憑證。相同輸入最多嘗試 AI 三次。
- 輸入未實質改變時不重複產稿、不重複呼叫 AI。金控收齊後仍繼續既有有上限重試的新聞／FVOCI 補充流程。
- 三個寫入流程共用 concurrency group，降低同時提交衝突。若仍發生 rebase 衝突，流程失敗，不強制覆寫。

## 手動修改內容

例如修改 2026/08：在 GitHub 打開 `docs/reports/2026-08.md`，按鉛筆編輯，直接提交修改。報告頁及 Excel 的「本月分析報告」會使用修改後的檔案。

系統以產出內容雜湊偵測人工修改，後續自動排程不覆蓋它。新的數據包仍會更新；若資料改變，報告頁提示「資料已有更新，本文保留人工修訂」。

想放棄人工修改並重新產稿：GitHub Actions → **Generate Monthly Analysis** → **Run workflow** → period 輸入 `2026/08`，勾選 overwrite_manual。只能指定單一月份，不允許批次覆蓋人工稿。

檔案：

- `YYYY-MM.md`：可直接修改的正式報告。
- `YYYY-MM.pack.json`：完整分析資料包（西元年月、億元；含歷史、新聞與來源）。
- `YYYY-MM.meta.json`：輸入／輸出版本、AI 狀態、人工修改偵測資訊；通常不需修改。
- `index.json`：報告索引，由產生器維護。

公開儲存庫中的所有報告與資料包均為公開資料；公司內部管理帳留在公司 Copilot 環境。

## 每月 Excel

仍由首頁「下載 Excel」取得。保留金控、銀行、壽險、證券、其他及市場原表；另含：

- 分析使用說明與可供 Copilot 使用的分析指令。
- 金控／子公司分析指標、前月及前三月基準、近三月合計、排名資格與註記。
- 子公司變動拆解及其他殘差；子公司資料缺漏或跨合併不估算拆解。
- 截至所選月份的近 24 月金控及所有子公司原始數據；缺月列資料不足，不帶入未来數字。
- 近 24 月新聞摘要、來源標題及 URL、已提供的發布日期；摘要生成時間與發布日期分開。
- 市場歷史（指數、匯率、殖利率 bps、台股日均成交額等）。
- FVOCI 補充揭露及計算差額，金控／壽險的累計 YoY 皆留白。
- 本月完整分析報告（包含人工修訂）。

新增分析工作表固定億元、小數一位；Excel 儲存原始計算精度，百分比為可運算的數值。原月報工作表仍跟隨使用者所選單位。

## 新聞與限制

現有新聞摘要是二次整理，不等同逐句核實的公司原始新聞稿。報告保留来源及明確歸屬；沒有發布日期時寫「未取得」，不以摘要產生時間代替。來源內容與數字矛盾時不得當成獲利原因。

FVOCI「加計差額」是同月加計後數減原始淨利，不冒稱獨立取得的處分利益明細。門檻型資料不計精確差額。市場變化不直接等同損益因果。

## 本機驗證

```text
node tests/analytics.test.cjs
node tests/analysis-pack.test.cjs
python -m unittest discover -s tests -p "test_*.py"
node tests/export-smoke.cjs .test-output/xlsx.cjs
python scraper/monthly_report.py --all --no-ai
python scraper/monthly_report.py --period 2026/08
```

最後一個命令僅在環境已有 `ANTHROPIC_API_KEY` 時呼叫 AI；不自動載入本機 `.env`。
