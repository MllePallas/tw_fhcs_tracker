# 完整月報、趨勢重點與比較口徑

2026-09 更新。此文件取代舊版「只看期末月份的併購 cutoff」與「加計 FVOCI 對原始損益計 YoY」規則。

## 使用方式

首頁預設「完整月報」，作為管理層第一層資料；保留「趨勢重點」與「期間比較」。

- 摘要優先列轉盈／轉虧、月增但仍低於近期均值、連續變化，以及偏離均值較大的公司。摘要是明確規則排序，沒有綜合評分。
- 趨勢總表可依月增加／月減少金額、當月金額與所選指標的偏離幅度排序；近期比較的零值、負基期與不可比狀態不以百分比排序；YoY 按有效變動率排序。
- 表內觀察指標可切換前 3 月平均、近 3 月比較、單月 YoY。公司迷你圖採獨立縱軸，缺值或口徑變動斷線。
- 點公司或熱度格可檢查對應月份、金額、基準、差額、公告連結。公司圖的 3 月移動平均包含當月，總表的「前 3 月平均」不含當月。
- 下方增減圖使用絕對金額；熱度表使用各月相對自身前 3 月均值的百分比，±50% 為色階飽和點。兩者跟隨公司篩選。
- 市場概況與原有四產業獲利第一名移至頁尾可展開區域。
- Excel 新增趨勢重點、歷史原始數據與比較口徑，保留完整月報工作表。趨勢工作表跟隨篩選，完整工作表保留所有公司。金額及有效百分比為 Excel 數值。圖片依目前模式輸出；完整月報圖片補上 MoM。

## 比較規則

`docs/comparison-rules.json` 是前後端共用的事件與門檻設定。

- 計算統一轉為百萬元。MoM 比較兩個單月；均值比較當月與不含當月的前 3 月平均；近 3 月比較兩段互不重疊的單月加總。
- 單月、累計及期間 YoY＝（本期−去年同期）÷去年同期絕對值 × 100%。原始稅後淨利一律列變動率，合併與會計新制另留註記，不取代數字。去年同期為零不計百分比，缺值不補零。
- MoM 與近期均值比較，基期為正且高於低基期門檻才顯示百分比。虧損縮小／擴大、跨零、零基期以狀態及差額取代成長率。
- 低基期門檻為 15 百萬元，或比較窗口單月絕對值中位數的 10%，取較高者。合計依基期月數調整，平均按單月。YoY 不套用低基期門檻。
- 任一必要月份缺值或公告期別不符即停算，不補零、不以其他月份替代。
- 台新新光 2025/07 為月中合併的過渡月；永豐金 2025/10 起納入京城銀行。MoM 與近期均值比較跨越事件時不計百分比。累計 YoY 仍按公告數計算，註記檢查兩年各自 1 月至期末的完整區間。
- 子公司使用獨立整併事件與未變動子公司例外；名稱別名僅代表同一實體更名。未知子公司消失／新增不假設零損益，也不製造增減歸因。
- 壽險新制自 2026/01 起生效。依使用者指定的分析師／公司端表達方式，金控與壽險原始稅後淨利 YoY 保留百分比；新制影響以額外註記呈現。MoM 與近期均值的跨制限制仍保留。事件來源附於 JSON 與網頁計算說明。
- FVOCI 加計數獨立呈現，不併入趨勢，不對原始損益計百分比；金控與壽險加計列的累計 YoY 真正留白（網頁、卡片、圖片及 Excel 均不放破折號或口徑文字）；門檻值保留「逾／突破」。當月是否揭露依當期資料，不使用固定公司註腳。
- 單月 EPS 僅列公告值，未揭露為空。期間 EPS 仍為累計差額推算，保留股本變動可能影響的提示。

## 累計核對與資料更正

趨勢採原始單月公告數；期間比較採期末累計減期初前月累計，一月起算直接取當期累計。跨年趨勢以單月加總，避免年度歸零。

公司詳情並列單月加總、公告累計與差額。合庫金原始單月含非控制權益而累計取母公司業主，差異以口徑說明處理；台新新光 2025/10–11 的已知累計調整另外標示。其他超過四捨五入容差的差額標為待核對，不自行改寫公告金額。

前端每次載入歷史資料後，在記憶體重算衍生比較，舊 JSON 內既存 YoY 欄位不直接採信。本次不重寫歷史公告檔案；爬蟲後續更新由 `scraper/comparison_policy.py` 產生相同規則的累計 YoY。原始單月、累計、EPS、FVOCI 金額保持不變。

## 維護與驗證

- `docs/analytics.js`：無 DOM 的純計算模組。
- `docs/trends.js` / `docs/trends.css`：趨勢畫面、互動、匯出。
- `docs/app.js`：既有月報／期間比較整合。
- `scraper/comparison_policy.py`：爬蟲衍生欄位規則，讀取同一份 JSON。

本機啟動：`python -m http.server 8765 --bind 127.0.0.1 --directory docs`。

測試：

```text
node tests/analytics.test.cjs
python -m unittest discover -s tests -p "test_*.py"
python -X utf8 scraper/validate.py --all --strict
```

實際 Excel 匯出測試：先將網站既有的 `xlsx-js-style@1.2.0/dist/xlsx.bundle.js` 下載到 `.test-output/xlsx.cjs`，執行 `node tests/export-smoke.cjs .test-output/xlsx.cjs`。會產生並重新讀回 `.test-output/taiwan-fhcs-2026-08.xlsx`，檢查數值型別、MoM 轉盈狀態、公告 YoY 變動率、壽險 FVOCI YoY 留白、缺值 EPS 及文字換行格式。此目錄不納入版本控制。

規則包含明確已知事件，不能自動判定尚未登錄的新併購或新會計政策。新增事件時需補設定與邊界測試。
